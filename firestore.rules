rules_version = '2';

// Clients never touch Firestore directly: the website and apps read through the
// callable functions, and only the Admin SDK (functions, scraper) writes.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
