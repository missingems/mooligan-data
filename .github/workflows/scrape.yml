name: Scrape MTGGoldfish

# Downloads the published snapshots from R2, scrapes what is new, and uploads
# the result. See docs/data-format.md for the files.
on:
  schedule:
    - cron: "0 2,14 * * *"
  workflow_dispatch:
    inputs:
      formats:
        description: Formats to scrape
        default: modern,standard,pioneer,legacy,pauper,vintage,premodern,penny_dreadful,duel_commander
      max_new_decks:
        description: New decklists per format per run
        default: "1500"

# Two runs must never publish over each other.
concurrency:
  group: scrape
  cancel-in-progress: false

env:
  AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
  AWS_DEFAULT_REGION: auto
  R2: s3://mtg-meta-data
  R2_ENDPOINT: https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    defaults:
      run:
        working-directory: scraper
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: scraper/requirements.txt
      # The runner image ships Google Chrome; UC mode runs it headed inside Xvfb.
      - run: sudo apt-get update && sudo apt-get install -y --no-install-recommends xvfb
      - run: pip install -r requirements.txt

      - name: Download the previous snapshots and state
        # sync succeeds on an empty bucket, so the first run starts from nothing.
        # state holds what each stored deck plays, which the card pages are built from.
        # Three tries: a dropped connection mid-sync failed a whole run once, and sync picks up
        # where it left off.
        run: |
          sync() {
            for attempt in 1 2 3; do
              aws s3 sync "$1" "$2" --endpoint-url "$R2_ENDPOINT" && return 0
              echo "::warning::sync of $1 failed (attempt $attempt of 3)"
              sleep 15
            done
            return 1
          }
          sync "$R2/snapshots" work/snapshots
          sync "$R2/state" work/state

      - name: Scrape
        id: scrape
        continue-on-error: true
        env:
          FORMATS: ${{ inputs.formats || 'modern,standard,pioneer,legacy,pauper,vintage,premodern,penny_dreadful,duel_commander' }}
          MAX_NEW_DECKS: ${{ inputs.max_new_decks || '1500' }}
          META_DAYS: "30,14,7"
          REQUEST_DELAY: "1"
          CONCURRENCY: "6"
          # EDHREC is read with their permission: a slice of the cards each run.
          EDHREC_CARDS_PER_RUN: "800"
          EDHREC_COMMANDERS_PER_RUN: "150"
          # Upcoming events near these places, from Wizards' locator; semicolon-separated.
          LOCATOR_PLACES: "Singapore"
          LOCATOR_DISTANCE_MILES: "15"
          LOCATOR_DAYS: "14"
          EDHREC_DELAY: "0.4"
        run: python -m mtgmeta work

      # finish() writes index.json last, so its presence means the run got as far as publishing.
      - name: Upload to R2
        if: always() && hashFiles('scraper/work/index.json') != ''
        run: |
          cp() { aws s3 cp "$@" --endpoint-url "$R2_ENDPOINT" --content-type application/json --only-show-errors; }
          # Decks first and the index last, so nothing published points at a file not yet uploaded.
          if [ -d work/decks ]; then
            cp work/decks "$R2/decks" --recursive --cache-control "public, max-age=31536000, immutable"
          fi
          if [ -d work/cards ]; then
            cp work/cards "$R2/cards" --recursive --cache-control "public, max-age=300"
          fi
          if [ -d work/edh ]; then
            cp work/edh "$R2/edh" --recursive --cache-control "public, max-age=300"
          fi
          if [ -d work/locator ]; then
            cp work/locator "$R2/locator" --recursive --cache-control "public, max-age=300"
          fi
          if [ -d work/premier ]; then
            cp work/premier "$R2/premier" --recursive --cache-control "public, max-age=300"
          fi
          cp work/snapshots "$R2/snapshots" --recursive --cache-control "public, max-age=300"
          cp work/state "$R2/state" --recursive --cache-control "no-cache"
          cp work/index.json "$R2/index.json" --cache-control "public, max-age=60"
          echo "Published: $(ls work/decks 2>/dev/null | wc -l) new decks, $(ls work/cards 2>/dev/null | wc -l) card pages"
          cat work/index.json

      - name: Fail if the scrape reported errors
        if: steps.scrape.outcome == 'failure'
        run: |
          echo "The scrape logged errors; what it did collect was published. See the Scrape step."
          exit 1
